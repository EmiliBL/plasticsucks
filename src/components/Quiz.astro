---
import { useState } from 'react';
---
<head>
  <title>Flip Card Quiz</title>
</head>
<body style="font-family: var(--font-sans-2), sans-serif" class=" font-family: var(--font-sans-2), sans-serif flex flex-col items-center justify-center min-h-screen bg-primary">
    <div
        id="card"
        class="font-family: var(--font-sans-2), sans-serif text-secondary w-[90%] max-w-[700px] h-[200px] md:h-[300px] flex items-center justify-center bg-primary text-center text-lg md:text-2xl cursor-pointer transition-transform duration-500 hover:bg- font-sans"
        onclick="window.flipCard()"
        style="font-family: var(--font-sans-2), sans-serif"style="font-family: var(--font-sans-2), sans-serif"
    >
        Start quizzen
    </div>
    <div
        class="mt-5 hidden"
        id="buttons"
    >
        <button 
            style="font-family: var(--font-sans-2), sans-serif" class=" px-4 py-2 mx-2 text-sm md:text-lg border-lightgrey-2px bg-primary text-secondary cursor-pointer hover:bg-darkgrey"
            onclick="window.nextQuestion()"
        >
            Ja
        </button>
        <button
           style="font-family: var(--font-sans-2), sans-serif" class="px-4 py-2 mx-2 text-sm md:text-lg border-lightgrey-2px text-secondary bg-primary cursor-pointer hover:bg-darkgrey"
            onclick="window.nextQuestion()"
        >
            Nej
        </button>
    </div>
</body>

    <script>
      interface Window {
        flipCard: () => void;
        nextQuestion: () => void;
      }
      const questions = [
        "Må man smide vatrundeler i toilettet?",
        "Indholder snus plastik?",
        "Ses vi på Valby strand d.18 Juni?",
      ];
      let currentQuestion = 0;
      document.addEventListener("DOMContentLoaded", () => {
        const card = document.getElementById("card");
        const buttons = document.getElementById("buttons");

        window.flipCard = function () {
          card.innerText = questions[currentQuestion];
          buttons.classList.remove("hidden");
        };

        window.nextQuestion = function () {
          currentQuestion++;
          if (currentQuestion < questions.length) {
            card.innerText = questions[currentQuestion];
          } else {
            card.innerText = "Tak fordi du gør en forskel i verden!";
            buttons.classList.add("hidden");
          }
        };
      });
    </script>
  </body>
</html>
